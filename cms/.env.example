# CMS Configuration
CONTENT_SOURCE_DIR=../sitio/content
DB_FILE=./db/cms.sqlite

# API Configuration
PORT=3001
API_URL=http://localhost:3001

# Schema Generation Prompt
PROMPT_MARKDOWN_SCHEMA="Analiza los archivos Markdown en la ruta especificada. Para cada tipo de contenido, identifica todas las propiedades del frontmatter y el cuerpo. Define un esquema SQL relacional donde: 1. Cada tipo de contenido es una tabla. 2. Cada propiedad es una columna con el tipo de dato adecuado. 3. Las relaciones usan GUIDs. 4. El cuerpo del markdown va en la columna 'Content'. 5. Normaliza estados y categorías."

# Database Credentials (Future Use)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=cms_prod
POSTGRES_USER=admin
POSTGRES_PASSWORD=secret
